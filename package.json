{
  "name": "realtime-1v1-fighter",
  "version": "1.0.0",
  "private": true,
  "description": "Real-time 1 vs 1 canvas fighting game with Socket.IO",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --check server.js && node --check public/game.js && node --check public/team-mode.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
